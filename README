filemail
By: Daniel Flehner Heen

Pyhton wrapper for www.filemail.com API
using requests by Kenneth Reitz.

import filemail


user = filemail.login('user@mailprovider.com')

transfer = Transfer(login,
                    to='lucky@receiver.com',
                    subject='My first filemail transfer',
                    message='This is awesome!',
                    notify=True,
                    confirmation=True,
                    days=7,
                    password='')

transfer.addfile('/path/to/myfile.ext')
transfer.complete()
transfer.get()

user.getReceived(age=1)
sent = user.getSent()
print sent

print user.getInfo()

user.save()
user.logout()